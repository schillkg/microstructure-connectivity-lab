export function viewerPanel(id,expanded,{image,link,url}) {
 const choices=[['af','Arcuate','#f3b64f'],['cst','Corticospinal','#49aaff'],['cc','Callosal','#ff4f99']];
 const stage=`<div class="viewer-stage"><canvas id="${id}-canvas" data-source="${url('/assets/glass-viewer.json')}" ${expanded?`data-centroids="${url('/assets/tract-centroids.json')}"`:''} tabindex="0" aria-label="White matter pathways inside a transparent brain. Drag or use arrow keys to rotate." role="img"></canvas>${image('/assets/tractography-detail.webp','Reconstructed white matter pathways inside the matching transparent brain','viewer-fallback').replace('<img ','<img data-viewer-fallback hidden ')}<p data-viewer-status role="status">Loading pathways…</p><noscript>${image('/assets/tractography-detail.webp','White matter pathways inside a transparent brain')}</noscript></div>`;
 if(!expanded)return `<div class="tract-viewer inline-viewer" data-tract-viewer>${stage}<div class="viewer-toolbar"><div class="tract-choices" role="group" aria-label="Choose pathways">${[['all','All'],...choices].map(([value,name])=>`<button data-bundle="${value}" aria-pressed="${value==='all'}">${name}</button>`).join('')}</div><div class="viewer-options"><button data-glass aria-pressed="true">Glass brain</button><button data-reset>Reset</button></div></div><div class="viewer-caption viewer-explore"><div><strong>Explore the pathways</strong><span>Change colors, compare centroids, and rotate the glass brain.</span></div>${link('/tractography/','Open full explorer <span aria-hidden="true">↗</span>','button explorer-link')}</div></div>`;
 return `<div class="tract-viewer expanded-viewer" data-tract-viewer data-expanded><div class="viewer-workspace"><div class="viewer-main">${stage}<div class="camera-toolbar"><div role="group" aria-label="Camera view">${[['oblique','Oblique'],['front','Front'],['side','Side'],['top','Top']].map(([value,name])=>`<button data-view="${value}" aria-pressed="${value==='oblique'}">${name}</button>`).join('')}</div><div><button data-spin aria-pressed="false">Rotate</button><button data-snapshot>Save image</button><button data-fullscreen>Full screen</button></div></div></div><aside class="viewer-settings" aria-label="Viewer settings"><div class="settings-heading"><h2>Pathways</h2><button data-reset>Reset</button></div><fieldset class="bundle-settings"><legend class="sr-only">Visible pathways and colors</legend>${choices.map(([value,name,color])=>`<div class="bundle-setting"><label><input type="checkbox" data-visible="${value}" checked><span>${name}</span></label><input aria-label="${name} color" type="color" data-color="${value}" value="${color}"></div>`).join('')}</fieldset><label class="viewer-field">Show<select data-render-mode><option value="fibers">Streamlines</option><option value="both">Streamlines + centroids</option><option value="centroids">Centroids</option></select></label><label class="viewer-field">Color by<select data-color-mode><option value="bundle">Pathway</option><option value="direction">Local orientation</option></select></label><p class="orientation-key" data-direction-key hidden><span>Red · left/right</span><span>Green · front/back</span><span>Blue · up/down</span></p><div class="settings-divider"></div><h2>Anatomy</h2><label class="viewer-check"><input type="checkbox" data-glass checked>Glass brain</label><label class="viewer-field">Surface opacity<input data-opacity type="range" min="0" max="100" value="30" aria-label="Brain surface opacity"></label><div class="settings-divider"></div><h2>Appearance</h2><label class="viewer-field">Tube width<input data-radius type="range" min="5" max="30" value="16.5" step=".5" aria-label="Streamline tube width"></label><label class="viewer-field">Lighting<input data-light type="range" min="55" max="150" value="105" aria-label="Lighting brightness"></label><label class="viewer-field">Zoom<input data-zoom type="range" min="65" max="220" value="100" aria-label="Zoom"></label><label class="viewer-field">Background<select data-background><option value="dark">Ink</option><option value="light">Ivory</option></select></label></aside></div></div>`;
}
